{
  "scripts": {
    "build": "npm run minify:css && npm run minify:js",
    "minify:css": "cleancss -o styles.min.css styles.css",
    "minify:js": "terser music-player.js -o music-player.min.js && terser sw.js -o sw.min.js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "firebase-tools": "^15.12.0",
    "terser": "^5.46.1"
  }
}
